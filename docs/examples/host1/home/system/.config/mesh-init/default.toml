# default.toml — Applied to the main command in execution mode.
# Sets the UID/GID, resource limits, and environment for the main process.
[service]
name = "default"
command = "_placeholder_"
priority = 0

[resources]
memory_max = "256M"
cpu_weight = 100

[environment]
RUST_LOG = "info"
