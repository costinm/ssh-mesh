#!/usr/bin/env sh
set -eu

self_dir="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
virt="$(CDPATH= cd -- "${self_dir}/.." && pwd)"

export VIRT="${VIRT:-${virt}}"
export PATH="${virt}/bin:${PATH}"

exec "${virt}/bin/vrun.lib" "$@"
