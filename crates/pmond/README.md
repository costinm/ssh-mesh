# Process monitor

The create intent is to watch cgroups and detect pressure, running as as a UID
with appropriate permissions. Some of this is moving into or used by mesh-init.

It also provides a web interface for demo/devel.