[package]
name = "mesh-mcp"
version = "0.1.0"
edition = "2024"

[dependencies]
mesh = { path = "../mesh" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
