[Service]

ExecStart = "./mesh9p /tmp"
OOMScoreAdjust = -600
[Resources]
memory_max = "128M"
cpu_weight = 50

[Environment]
RUST_LOG = "info"

# Socket activation is declared in mesh9p-listener-uds.socket.
